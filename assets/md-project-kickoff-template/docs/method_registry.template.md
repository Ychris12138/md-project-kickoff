# Method Registry

Last updated: `<YYYY-MM-DD>`

This file answers: which method/script is currently authoritative?

## 1. Methods

| Method | Scientific purpose | Canonical implementation | Inputs | Outputs | Status | Replaces/related legacy |
|---|---|---|---|---|---|---|
| `<method name>` | `<what it measures>` | `<script/module path>` | `<input files>` | `<outputs>` | idea/design/active/canonical/legacy | `<old files>` |

## 2. Status Meanings

- `idea`: only a question or direction exists.
- `design`: contract or method design is being clarified.
- `active`: implementation or trial runs are underway.
- `canonical`: current recommended method.
- `legacy`: retained as reference, not default.

## 3. Method Notes

### `<method name>`

- Current question:
- Current entry point:
- Key definitions:
- Validation:
- Known risks:
- Next action:

## 4. Migration Notes

When moving methods from another project:

| Source project | Source files | Target files | Definitions copied | Validation | Status |
|---|---|---|---|---|---|
| `<source>` | `<files>` | `<files>` | `<definitions>` | `<checks>` | planned/done |

## 5. Legacy / Debug Files

| File/folder | Why kept | Replacement | Safe to ignore by default? |
|---|---|---|---|
| `<path>` | `<reason>` | `<canonical path>` | yes/no |

