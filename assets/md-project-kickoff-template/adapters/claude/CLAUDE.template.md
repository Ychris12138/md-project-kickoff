# Project Agent Instructions

@PROJECT_INDEX.md
@AGENTS.md

`AGENTS.md` is the canonical shared project workflow. Follow it for local Git,
analysis contracts, literature notes, output locations, SSH boundaries, Slurm
logs, smoke tests, result retrieval, and cleanup approval.

Before remote execution, read the relevant workflow under `docs/codex/` and
wait for explicit user approval after presenting the review package.
